server:
  port: 8083
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Department-serviceDB
    username: root
    password: Wangla11pb
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate.ddl-auto: update
    show-sql: true
  

eureka:
  instance:
    client:
      serverUrl:
        defaultZone: http://localhost:8761/eureka/

